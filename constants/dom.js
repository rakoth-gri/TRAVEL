const sliderContainer = document.querySelector(".popular__slider"),
	sliderTracker = sliderContainer.querySelector(".popular__slider__tracker"),  
	sliderDotes = sliderContainer.querySelector(".slider__dotes");

export { sliderContainer, sliderTracker, sliderDotes };
